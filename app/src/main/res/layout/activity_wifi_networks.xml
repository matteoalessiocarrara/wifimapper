<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_wifi_networks"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.carrara.wifimapper.WifiNetworksActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/wifi_networks_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        android:elevation="4dp"
        android:theme="@style/ThemeOverlay.AppCompat.Dark" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/wifi_networks_recycler_view"
        android:scrollbars="vertical"
        android:layout_below="@+id/wifi_networks_toolbar"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

</RelativeLayout>
